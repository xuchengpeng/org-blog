#+TITLE: Debian 安装 fcitx5-rime 输入法
#+AUTHOR: Chuck
#+DESCRIPTION: 在 Windows 或 WSL Debian 中都是通过小狼毫和 emacs-rime 来使用 RIME 输入法的，笔记本电脑的 Debian 13 KDE 桌面环境中，就使用当前很流行的 fcitx5-rime 输入法。
#+KEYWORDS: Linux, Debian, Arch, RIME
#+DATE: <2026-02-06 Fri 14:59>

在 Windows 或 WSL Debian 中都是通过小狼毫和 emacs-rime 来使用 RIME 输入法的，笔记本电脑的 Debian 13 KDE 桌面环境中，就使用当前很流行的 fcitx5-rime 输入法。

首先通过以下命令进行安装：

#+begin_src bash
sudo apt install fcitx5 fcitx5-chinese-addons fcitx5-rime rime-data-double-pinyin kde-config-fcitx5
#+end_src

Arch Linux 的安装命令为：

#+begin_src bash
sudo pacman -S fcitx5 fcitx5-gtk fcitx5-qt fcitx5-configtool fcitx5-rime rime-double-pinyin
#+end_src

fcitx5-rime 的输入法配置文件目录为 =~/.local/share/fcitx5/rime= ，这里面存放自定义的输入方案配置。

打开系统虚拟键盘设置，选择 fcitx5 。

打开 fcitx5 配置，添加 RIME 到当前输入法中，全局配置中可以设置切换中英文输入法的快捷键，附件组件中还可以选择不同的主题。

重新启动，就可以使用 RIME 输入法了，经过测试终端、Emacs、 Chrome中都可以正常使用。

#+begin_quote
Do not set ~GTK_IM_MODULE~ & ~QT_IM_MODULE~ & ~SDL_IM_MODULE~. You could unset ~GTK_IM_MODULE~ & ~QT_IM_MODULE~ by runing ~im-config~ and then selecting "do not set any IM from im-config and use desktop default".
#+end_quote

修改输入方案配置后，需要重新部署以生效，可以在输入法界面中点击重新部署按钮，也可以通过命令行重新部署：

#+begin_src bash
rm ~/.local/share/fcitx5/rime/default.yaml && fcitx5-remote -r
#+end_src
